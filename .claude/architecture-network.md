# TotalShooter — архитектура: мультиплеер, репликация, сессия

Часть детальных архитектурных решений с обоснованиями. Разделено на три файла по теме — не
грузятся автоматически, читать по необходимости:
- [`architecture-core.md`](architecture-core.md) — Entity, World/сохранения, реестр контента,
  скриптинг, компоненты, прогресс игрока, права.
- **`architecture-network.md`** (этот файл) — мультиплеер, репликация, сессия, UI.
- [`architecture-combat.md`](architecture-combat.md) — боёвка, hit-registration, лут/килстрики.

Короткая версия без обоснований — `CLAUDE.md` в корне проекта.

## Мультиплеер: Unity Netcode for GameObjects + Unity Relay

Сетевая модель — player-hosted (listen server): один из игроков хост, авторитетно
симулирует игру, остальные подключаются через Unity Relay (NAT traversal, не нужен свой
выделенный сервер и его хостинг). Транспорт/репликация — Unity Netcode for GameObjects (NGO),
официальное и бесплатное решение Unity (платится только фактический трафик Relay).

Кто использует NGO `NetworkObject`, а кто — только наш собственный `EntityId` — см. отдельный
раздел "Сетевой статус Entity" ниже.

**Почему:** ключевая ось масштабирования в этом проекте — не количество *игроков* в сессии
(коопный отряд, обычно немного), а количество *AI-юнитов*. CCU-биллинг сторонних SDK (Photon)
поэтому не является проблемой при таком составе сессий, но NGO+Relay даёт то же самое
бесплатно и official-supported, что проще. Полноценный DOTS Netcode for Entities или Photon
Quantum (deterministic lockstep) дали бы больше "из коробки" под масштаб тысяч юнитов, но
требуют полного перехода геймплейной логики на ECS/детерминированную математику — это
противоречит решению по "Массовые юниты: сначала ООП" (`architecture-core.md`) и требованию
к простоте.

**Запасной вариант**, если NGO упрётся в производительность даже при небольшом числе героев/
мета-объектов сессии — **FishNet** (open-source, больше контроля над репликацией). Не начинаем
с него, чтобы не платить сложностью заранее.

## Миграция хоста: осознанно не делаем

При выходе хоста из сессии — **сессия просто завершается**, все отключаются. Никакой
автоматической/бесшовной миграции роли хоста на другого игрока не строим.

**Почему:** хост в нашей архитектуре — единственный источник истины одновременно и по сети
(NGO-сервер), и по геймплейной логике (`GameManager`/`AIController`/`AIDirector`, живые
Lua-скрипты в `Controller`, нереплицированные по умолчанию `EntityVariables`, реестр
`EntityId` в `WorldManager`). У NGO нет встроенной поддержки передачи роли сервера другому
клиенту. Настоящая бесшовная миграция потребовала бы либо реплицировать клиентам заметно
больше состояния, чем нужно для рендера (конфликтует с решением экономить трафик — Lua/
`EntityVariables` намеренно host-only), либо смириться с "мягким рестартом" систем при
передаче роли (Lua-скрипты перезапускаются с нуля, экономика AIDirector обнуляется).
Даже Minecraft (наш референс для формата мира) не решает эту проблему в ванильном
LAN-хостинге — просто требует быстрого рестарта сервера.

**Восстановление на практике** — не автоматическое, а ручное: кто-то из игроков поднимает
новый хостинг из последнего автосейва мира (уже решённая механика), при необходимости получив
файл мира от других вручную (см. "Механизм скачивания/передачи мира" ниже — миры передаются
вручную). Никакой новой инженерии не требуется.

**Известное ограничение:** автосейв пишется в папку мира на диске хоста, а не рассылается
остальным игрокам автоматически — если хост стал недоступен без предупреждения, у других
игроков может не быть самой свежей копии мира (только та, что скачивали раньше). Приемлемо
для игры с друзьями; пересмотреть, если это когда-нибудь станет реальной проблемой.

## Сетевой статус Entity: EntityId (наш) vs NetworkObject (NGO)

Единое правило для всех `Entity`, без исключений:

- **У каждого `Entity` есть собственный `EntityId`** (наш ID, не NGO-шный `NetworkObjectId`).
  Это база, есть всегда. По нему адресуются команды/события в кастомном канале репликации
  (см. "Репликация мобов" ниже) и Lua-скрипты обращаются к конкретному Entity.
- **`NetworkObject` — опциональная надстройка NGO**, вешается только на то, что реально
  нуждается в индивидуальном авторитетном владении через NGO: **герои игроков** и
  **мета-объекты сессии** (`GameState`, `PlayerState`, `PlayerController`).
- **Всё остальное в игровом мире — просто `Entity` с `EntityId`, без `NetworkObject`**: мобы,
  турели/баррикады/ловушки/мины, лут, снаряды. Реплицируется единым кастомным каналом
  (событие спавна + редкие апдейты состояния), независимо от того, "штучный" это объект или
  один из тысячи.

**Почему так, а не "важные штучные объекты — NetworkObject, толпа — нет":** турели/баррикады
не двигаются — им не нужен даже dead reckoning, только событие спавна + редкие апдейты
HP/состояния. Они попадают в ту же машинерию, что и мобы, практически бесплатно, раз мы её
всё равно строим для мобов. Не плодим два параллельных механизма репликации ("важное штучное"
и "толпа") — один кастомный канал для всего в игровом мире, кроме героев, и NGO только для
героев + мета-объектов сессии.

### Три профиля репликации (почему нет дилеммы "синхронизировать всё vs слишком много")

Кажущийся конфликт — "в песочнице хотим уметь синхронизировать ЛЮБУЮ Entity (даже позицию
стены), но Entity сотни-тысячи, в лоб нельзя" — растворяется, если разделить **количество**
объектов и **частоту/тип** их изменений. "Синхронизировать любую Entity" ≠ "непрерывно стримить
все Entity": стена двигается не каждый кадр, а только когда её отредактировали — это редкое
**событие**, и его стоимость зависит от числа правок, а не от числа объектов (тысяча нетронутых
стен стоят по сети ноль). Отсюда три профиля, каждому свой инструмент:

| Профиль | Кто | Кол-во | Тип синка | Механизм |
|---|---|---|---|---|
| Непрерывный | Герои игроков | Десятки | Позиция каждый тик, плавно | NGO `NetworkObject` + owner-auth `NetworkTransform` |
| Событийный | Редактируемые (стены, турели, баррикады, лут) | Много | Редкое событие правки/урона | Кастомный reliable event-канал |
| Командный | Мобы | Тысячи | Редкие команды + допуск | Кастомный command-канал + dead reckoning (ниже) |

**Движение игрока — owner-authoritative `NetworkTransform`.** Пешка героя — `NetworkObject`;
владелец считает своё движение/прыжки **локально** (нулевой инпут-лаг, на своём экране никогда
не "мажет мимо платформы"), `NetworkTransform` стримит итоговый трансформ с интерполяцией,
остальные видят гладко и корректно (рендерят чуть в прошлом, буфер снапшотов). Именно эту
тяжёлую часть (интерполяция/экстраполяция/джиттер-буфер) `NetworkTransform` даёт бесплатно —
поэтому героев (их десятки) держим на NGO, а не переизобретаем это на кастомном канале. Урон
всё равно валидирует хост отдельно (lag-comp по хитам, не по движению), см. `architecture-combat.md`.

**"Всё есть Entity" остаётся истиной на уровне логики.** Репликация — отдельный слой-стратегия
под Entity: игровой код всюду адресует по `EntityId`, а то, что у героя под капотом ещё и
`NetworkObject`, — деталь его стратегии репликации, не второй концептуальный мир. Следствие:
сменить стратегию репликации конкретной Entity потом дёшево (адресация не меняется).

## Репликация мобов: command-channel + dead reckoning вместо постоянного снапшота позиции

Движение/pathfinding моба — **локальная логика**, исполняется независимо на каждом клиенте
(и на хосте). AI на хосте не решает "куда шагнуть в этом кадре" — он раз в 200-500мс (см.
"Массовые юниты: сначала ООП" в `architecture-core.md`) принимает решение уровня "иди в точку
X" / "атакуй Y" / "используй абилку Z" и рассылает это как **команду** через reliable
event-канал (тот же, что для гранат/хила/смерти). Моб на клиенте сам исполняет команду (свой
локальный pathfinding/steering), а не ждёт постоянных апдейтов позиции.

Поверх этого — редкая (~1 раз/сек) авторитетная позиция с хоста для коррекции дрифта
(dead reckoning / client-side extrapolation + periodic reconciliation, стандартный паттерн
для реплицируемых NPC/техники в шутерах). Коррекция — только если расхождение вышло за
допустимый радиус погрешности (не дёргать позицию за каждую мелочь). Точный алгоритм
коррекции — при реализации.

**Почему:** это не RTS, точная синхронизация мобов между клиентами не нужна (три моба могут
стоять треугольником на одном клиенте и линией на другом — не важно). Важно ощущение
"игра работает правильно", а не побитовое совпадение. Такой подход резко снижает сетевой
трафик по сравнению с постоянной трансляцией позиции/направления каждого моба, и совпадает
с уже выделенной "горячей" подсистемой движения (тот же `MovementManager` — кандидат на
Job/Burst позже).

Нюанс на будущее (не сейчас, территория задачи навигации): если карта редактируется в
реалтайме (sandbox), локальный pathfinding клиента должен видеть те же изменения навигации,
что и хост.

## Жизненный цикл сессии: GameManager / SessionManager / GameState

Разделение ответственности (по мотивам UE GameMode/GameState, адаптировано под наш стек):

- **`GameManager`** — host-only "мозг матча", НЕ networked-объект сам по себе (как
  `AIController`/`AIDirector` — клиентская копия не нужна). Отвечает за жизненный цикл сессии:
  lobby → ready-up/countdown → игра идёт → (возможно) итоги. Решает, когда включены
  `AIController`/`AIDirector`, когда игрокам выдаётся боевой персонаж вместо призрака, разрешён
  ли переход в sandbox. Дирижирует `AIController`/`AIDirector`/`WorldManager`, не подменяет их.
- **`SessionManager`** — только жизненный цикл сессии/комнаты: создать/найти/присоединить/
  покинуть сессию через Sessions API (Relay + Lobby под капотом, список комнат — см. раздел UI
  ниже). Не путать с движком репликации `Unity.Netcode.NetworkManager` (транспорт/спавн/синк) —
  это разные слои: `SessionManager` — «входная дверь» в сессию, NGO — движок. Спавн игроков на
  коннекте (`Player Prefab` = `PlayerState`+`PlayerController`) делает `GameManager`, подписываясь
  на коннект-коллбэки NGO, а не `SessionManager`. `GameState`/`AIController`/`AIDirector` —
  одиночные объекты на сессию, их тоже создаёт `GameManager` при старте хостинга.
- **`GameState`** — реплицируемое состояние матча, целиком **KV-blackboard** (а не набор
  фиксированных `NetworkVariable`-полей): фаза/countdown/число готовых и любую кастомную мету
  пишет матч-Lua (через `GameManager`), клиенты читают для UI. Пишет только хост. Реализация —
  `Blackboard : NetworkList<KvEntry>` (реплицируемый key-value, значения `string`/`double`/
  `bool` — нативные типы Lua), см. `code.md`.
- **`PlayerState`** — `NetworkObject`, по одному на игрока. Тоже KV-blackboard, но **две секции**
  с разным правом записи (в NGO нет прав «по ключу»): **owner-секция** (пишет владелец: имя,
  аватар, скин) + **host-секция** (пишет хост: счёт, флаги, ачивки). Это упрощение относительно
  UE (там `PlayerState` server-authoritative, клиент шлёт RPC) — осознанно, т.к. анти-гриферство
  не в приоритете; PvP-критичное кладём только в host-секцию.
- **`PlayerController`** — `NetworkObject`, но скрыт от всех клиентов кроме своего владельца
  через NGO network visibility (`NetworkShow`/`NetworkHide`) — у него в основном приватные для
  игрока данные (инпут, cooldown-ы абилок для UI и т.п.), другим игрокам это не нужно.

**Possession-паттерн (UE-стиль):** `PlayerController` не пересоздаётся при смене управляемого
персонажа — есть `Possess(Entity pawn)`/`Unpossess()`. Дефолтный призрак при входе → боевой
персонаж при старте матча → тот же (не новый) призрак при смерти, скрытый пока жив персонаж →
снова персонаж при респавне → золотой призрак при переключении в sandbox. `CameraOperator`
переключается вслед за текущей possessed Entity.

**Ready-up/countdown как скрипт (решено делать сразу так):** матч-логика (готовность → отсчёт →
старт) живёт в Lua-скрипте на хосте, который читает/пишет KV-`GameState`/`PlayerState`, а не в
жёстком C#. Ради этого `GameState`/`PlayerState` и сделаны KV-blackboard'ами, доступными из Lua
индексатором (как `EntityVariables`). `GameManager` — тонкий: хостит матч-скрипт + спавнит
игроков, фазы не хардкодит. (См. заметки на будущее ниже про скриптуемые UI-виджеты — тот же
вектор.)

## Редактирование в sandbox не-хостом

Раз большинство компонентов не сетевые (`Health`, `Spawner`, `EntityVariables` и т.д., см.
`architecture-core.md`), а `Entity` в общем случае не `NetworkObject` — правка конкретного
компонента игроком, не являющимся хостом, идёт тем же путём, что и любое другое игровое
действие в этой архитектуре: **клиент отправляет запрос на изменение хосту** ("установи
такому-то `EntityId` такое-то поле в такое-то значение"), **хост валидирует** (в перспективе —
по модели прав/групп, см. "Права редактирования в sandbox" в `architecture-core.md`)
**и применяет**, затем рассылает применённое значение наблюдателям через тот же кастомный
event-канал. Ничего нового изобретать не пришлось — редактирование в sandbox — просто ещё
одна категория команд/событий в уже спроектированном канале, наравне с атакой/гранатой/
спавном.

## UI: структура экранов

- **`UIMainMenu`** — стартовый экран: "Создать" → `UICreateGame`, "Присоединиться" →
  `UIJoinGame`, "Настройки" → `UISettings`, "Выход".
- **`UICreateGame`** — создание комнаты (название, пароль, выбор мира).
- **`UIJoinGame`** — список доступных комнат + подключение по паролю. Список комнат — через
  **Unity Lobby** (сервис Unity под список комнат/join-by-code, естественно сочетается с
  Relay, который уже выбран для транспорта).
- **`UIInGameHUD`** — открывается после `UICreateGame`/`UIJoinGame`, пока идёт матч.
- **`UILoadingScreen`** — единственный UI-объект, активный сразу на старте (на стартовой
  сцене). Переключается видимостью (`SetActive(true|false)`) поверх остальных экранов на
  время загрузки мира между `UICreateGame`/`UIJoinGame` и `UIInGameHUD`.

## Механизм скачивания/передачи мира (bulk-transfer)

Unity Transport (UTP), на котором работают NGO и Relay — message-oriented поверх UDP, не
потоковый сокет вроде TCP. Готовой функции "просто стримить файл" нет — нужно вручную бить
данные на чанки и пересобирать на другой стороне, но это стандартная, многократно решённая
задача, не требующая нового способа обхода NAT (используется уже установленное через Relay
соединение).

**Схема:** вся папка мира упаковывается в **один zip-архив** (проще, чем протокол
"список файлов → запрос каждого по отдельности" — мир передаётся целиком, а не по частям на
выбор). Архив режется на чанки консервативного размера (заведомо меньше практического лимита
сообщения UTP — точную цифру сверять с документацией конкретной версии пакета на момент
реализации) и передаётся последовательностью reliable-сообщений через NGO custom/named
messages (гарантируют доставку и порядок — ручной retry не нужен). Получатель дописывает
чанки в файл по мере получения, по завершении распаковывает архив в свою `Worlds/<name>/`.

**Область применения — вне активного матча.** Одновременная передача крупного архива по тому
же каналу, что и реалтайм-репликация геймплея, посадит FPS/лаг всем на время закачки.
Реалистичный сценарий использования — скачивание мира перед подключением/через меню (пока
матч не идёт, есть экран загрузки) — фича осознанно ограничивается этим случаем, проблема
совмещения с игровым трафиком не возникает и отдельно не решается.

## Синглплеер — не отдельный случай (подтверждено)

Раз архитектура host-authoritative, синглплеер — это просто сессия с одним игроком-хостом
без подключённых клиентов. Отдельного "оффлайн-режима" проектировать не нужно.

## Заметки на будущее (не решено, не блокирует текущую работу)

- **Дистрибуция через Steam** (и как следствие — Steam Workshop для передачи миров вместо
  своего bulk-transfer механизма) — сознательно не планируется сейчас.
- **Скриптуемые UI-виджеты (Lua сам создаёт кнопки/таймеры на экране игрока).** Идея: скрипт
  на хосте мог бы сам инстанциировать виджет "готов" у всех игроков и тикать таймер обратного
  отсчёта, а не полагаться на жёсткую C#-логику `GameManager`/`GameState`. Архитектурно это
  укладывается в уже принятые паттерны (host-authoritative Lua, события в обе стороны через
  кастомный канал) и могло бы даже переиспользовать концепцию `Entity`/`EntityId`
  (`architecture-core.md`) для самих UI-виджетов (виджет — тоже адресуемый по id объект с
  событиями, просто рендерится на Canvas, а не в 3D). Но это отдельная, довольно объёмная
  система (API спавна UI из Lua + маршрутизация кликов обратно в скрипт).
  **Рекомендация:** не строить её ради одного лишь ready-up — сначала сделать простой жёстко
  закодированный ready-up (см. "Жизненный цикл сессии" выше), чтобы быстрее дойти до
  играбельного прототипа, а "скриптуемые UI-виджеты" спроектировать позже как общую систему,
  если/когда понадобится не только для лобби.
