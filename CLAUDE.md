# TotalShooter — контекст проекта

Сендбокс-шутер, вид от третьего лица сверху (Alien Shooter / Alien Swarm стиль), кооп/PvP.
Оборона + захват областей карты, прокачка персонажа и оружия, лут, строительство
баррикад/турелей/ловушек/мин. Врагов много — от сотен до тысяч. Полное описание идеи и
геймплея — в `notes.txt`.

Sandbox-режим редактирования в духе Garry's Mod (любой объект можно отредактировать,
меню спавна) + внутриигровой скриптинг. Проект делается автором и друзьями (все —
геймдев-программисты/гейм-дизайнеры) для игры друг с другом, с идеей допиливать игру на
ходу, играя в неё — защита от гриферства/читерства сознательно не в приоритете сейчас.

Здесь — только выжимка решений, детали разложены по файлам в `.claude/` (не грузятся
автоматически, читать по необходимости для конкретной задачи):

- [.claude/architecture-core.md](.claude/architecture-core.md) — Entity, World/сохранения, реестр контента, скриптинг, компоненты, прогресс игрока, права
- [.claude/architecture-network.md](.claude/architecture-network.md) — мультиплеер, репликация, сессия, UI
- [.claude/architecture-combat.md](.claude/architecture-combat.md) — боёвка, hit-registration, лут/килстрики
- [.claude/code.md](.claude/code.md) — технические спецификации для реализации (поля/методы/потоки)
- [.claude/navigation_system.md](.claude/navigation_system.md) — навигационная система (area-node граф, LOS, звук/феромоны)
- [.claude/rampant_ai.md](.claude/rampant_ai.md) — исследование ИИ мода Rampant (Factorio)

Новые объёмные темы получают свой файл здесь же по мере проработки — не тащить их в общий
контекст, пока не нужны для конкретной задачи.

## Архитектура (кратко, без обоснований)

- **Entity** — плоский класс, без иерархии наследования. Композиция через компоненты/флаги
  (например `CanBeControlled`), не подклассы.
- **Массовые юниты** — обычный C# (MonoBehaviour-стиль) по умолчанию; точечный переход
  горячих подсистем (Movement, Targeting/LOS) на `Unity.Jobs`/Burst — только по факту узких
  мест. Централизованные менеджеры вместо `Update()` на каждом объекте, blittable-friendly
  данные в горячих компонентах.
- **Мультиплеер** — player-hosted (listen server) через Unity Netcode for GameObjects +
  Unity Relay.
- **Сетевой статус Entity** — `NetworkObject` только у героев игроков и мета-объектов сессии
  (`GameState`/`PlayerState`/`PlayerController`). Всё остальное (мобы, турели, баррикады,
  лут) — просто `Entity` со своим `EntityId`, без NetworkObject, один общий кастомный канал
  репликации на всё это.
- **Репликация мобов/структур** — command-channel (редкие команды от AI: иди/атакуй/абилка) +
  локальный pathfinding на клиенте + редкая (~1/сек) коррекция позиции от хоста
  (dead reckoning).
- **Урон/killstreak** — host-validated hits (lag compensation), не доверяем самоотчёту
  клиента. Одновременные смертельные удары нескольких игроков → всем assist.
  Хитбоксы простые (не skeletal), расширены на dead-reckoning допуск. Дробовик — честные
  дробинки-хитсканы с client-side `hitTest`-предфильтром (щедрый порог). Урон/тип — только
  по `weaponId` через реестр контента, не от клиента. Взрывы/AoE — без отмотки, с margin по
  max-пингу. Проджектайлы с осмысленным состоянием (граната) — Entity; простые пули — нет.
- **Лут** — эксклюзивно первому подобравшему, без дублирования.
- **Скриптинг** — Lua через NLua. Скрипт крутится в `Controller`, `EntityVariables` —
  key-value/blackboard компонент на любом Entity (по умолчанию не сетевой, конкретные ключи
  можно явно продвинуть в репликацию). API скриптам — white-list.
- **World** — карта = папка `Worlds/<world-name>/` (`world.json` + `data.json` +
  `Scripts/*.lua` + опциональные ассеты), не unity-сцена. Формат — JSON, с меткой
  `schemaVersion` (несовпадение — явный отказ загрузки, миграции пока не строим). Мир
  одновременно и шаблон, и сейв (различие только в состоянии) — "новая игра" копирует папку.
  Иерархия Entity — плоский список + `Parent` (EntityId), не вложенные `children`.
- **Реестр контента** — prototype-цепочки (инстанцировать префаб/прототип + наложить оверрайды
  из JSON), рекурсивно на N уровней, с защитой от циклов. Переопределение по тому же имени
  заменяет прототип глобально для мира (как в модах Factorio). Единая точка резолвинга имени.
- **Прогресс игрока** — аккаунт (локально у игрока, глобальные метрики) + профиль в мире
  (у хоста, метрики конкретного мира).
- **Права в sandbox** — группы в духе Unix (права объекта в одном `short`), sandbox-режим
  включает хост (для себя и/или других). Правку от не-хоста хост валидирует и применяет сам.
- **Сессия** — `GameManager` (host-only) ведёт лобби/countdown/матч и пишет в реплицируемый
  `GameState`; `NetworkManager` — только транспорт/комната/подключения. `PlayerController`
  скрыт от чужих клиентов, possession-паттерн переключения между пешками (призрак/персонаж).
- **Компоненты** — `Health` (с типами урона), `Spawner` (теги/префабы/кулдаун/автоспавн/лимит)
  как явные переиспользуемые компоненты.
- **Синглплеер** — не отдельный случай, просто хост без клиентов.
- **Миграция хоста** — осознанно не делаем. Хост вышел — сессия завершается, восстановление
  вручную из автосейва/переданного файла мира.
- **Кастомные ассеты/моды** — только через Unity AssetBundles, без рантайм-импорта сырых
  моделей (не строим свой пайплайн вроде glTFast).
- **Скачивание мира** — вся папка мира одним zip-архивом, чанками через NGO custom messages
  поверх Relay; только вне активного матча (экран загрузки/меню), не во время боя.

## TODO

### Сделано (архитектурные решения)

- [x] Мультиплеер: выбрать сетевое решение
- [x] Архитектура для массовых юнитов (сотни-тысячи врагов)
- [x] Внутриигровой скриптинг: выбрать инструмент
- [x] Прочие вопросы архитектуры (общий обзор пробелов)
- [x] Вижен по классам и объектам (сессия/сеть/компоненты) — см. .claude/architecture-network.md
- [x] Исследование мода Rampant (Factorio) — см. .claude/rampant_ai.md
- [x] Навигация, восприятие мобов (LOS) и передвижение игрока — см. .claude/navigation_system.md
- [x] Реестр контента со стабильными ID — prototype-цепочки, см. .claude/architecture-core.md
- [x] Формат файла мира: JSON
- [x] Сохранения — версионирование схемы (метка + отказ, миграции отложены), иерархия Entity (плоский список + Parent)
- [x] Миграция хоста — осознанно не делаем, восстановление вручную из сейва
- [x] Боевая система: hit-registration и lag-compensation в деталях
- [x] Рантайм-загрузка кастомных моделей — не делаем, только AssetBundles
- [x] Механизм скачивания/передачи мира — zip-архив чанками через NGO custom messages

### Открытые архитектурные вопросы

- [ ] AIDirector: дизайн + прогрессивная активация областей карты (после Rampant + nav-графа)

### Реализация (код) — см. .claude/code.md и .claude/navigation_system.md за деталями

Порядок ниже — по зависимостям (не браться за пункт, пока не сделано то, что выше в своей
группе). Кроме отдельно помеченной ветки нав-графа — она ни от чего в этом списке не зависит
(только от готового Entity) и может делаться параллельно с чем угодно ниже.

**Entity и компоненты (без сети, без реестра контента):**
- [x] Entity: базовый класс с EntityId и композицией компонентов
- [x] Подключить NLua в проект (NuGetForUnity + пакет NLua)
- [x] EntityVariables: key-value компонент + Lua-биндинг
- [x] Встраивание NLua: ScriptController (: Controller) хостит скрипт, white-list API (обнулить
      опасные глобалы — `os`/`io`/`debug`/`load_assembly`/`luanet` и т.п.). Базовый Controller —
      только possession, Lua не в нём.
- [x] Health компонент (с типами урона)
- [x] Item как компонент (не подкласс)
- [x] Ability класс
- [ ] Кастомный канал репликации Entity: command-channel + event + dead reckoning (дизайн можно
      начать раньше сети, но реально протестировать — только после NetworkManager)

**Нав-граф (независимая ветка, только от Entity):**
- [ ] Nav-граф: доработка структуры нод (форма/region-id/spatial index)
- [ ] Nav-граф: настоящий A* + движение "в пределах ноды"
- [ ] Nav-граф: массовый поиск пути (flow-field/Dijkstra-map)
- [ ] Nav-граф: LOS (трёхуровневая эвристика) и распространение сигналов

**Мир и реестр контента:**
- [x] WorldManager: загрузка/сохранение мира, реестр EntityId, резолвинг прототипов
- [x] SceneConverter: editor-тулза, конвертирует Unity-сцену (объекты с компонентом Entity и
      их компоненты) в файл мира — чтобы авторы могли накидывать первые версии миров в
      Unity-редакторе, пока нет внутриигрового редактора/генерации
- [ ] Spawner компонент (резолвит юнитов по имени через реестр контента WorldManager — раньше
      не имеет смысла)

**Пешки игрока (локально, ещё без сети) — схема Controller/Pawn/CameraOperator см. code.md:**
- [ ] Ghost pawn: обычный и золотой (sandbox) варианты — визуал на авторе, префаб есть
- [x] ActionMovement: исполнитель движения пешки (крутит CharacterController), принимает
      намерение `Move(worldDirection)` от контроллера. Враг позже — свой исполнитель (NavGridAgent).
- [ ] ActionMovement: добавить прыжок (Jump/Space) — тем же intent-паттерном (`Jump()` +
      вертикальная скорость поверх гравитации)
- [x] CameraOperator: доработать черновик — активный режим (Look/Zoom), следование за пешкой.
      Курсор/переключение режима — на стороне PlayerController.
- [x] PlayerController (локальная часть): читает ввод, раздаёт мышь→оператору, WASD/кнопки→пешке,
      possession призрак↔персонаж (сетевую часть — в группе «Сеть»)

**Сеть:**
- [x] Подключить Unity Netcode for GameObjects + Relay + Lobby (UPM-пакеты) — Relay+Lobby теперь
      единый пакет `com.unity.services.multiplayer`, использовать высокоуровневый Sessions API
      (`MultiplayerService.Instance.CreateSessionAsync`/`JoinSessionByCodeAsync`), не ручной Relay-флоу
- [x] NetworkManager: сессия через Sessions API (Relay+Lobby под капотом) — POCO-сервис через
      Service<T>. Спавн игроков на коннекте — в GameManager.
- [x] GameState: NetworkObject с фазой матча
- [ ] GameManager: состояние матча и оркестрация
- [ ] PlayerState + PlayerController: NetworkObject, possession-паттерн

**Сборка сессии и UI (нужен весь стек выше):**
- [ ] UI меню: UIMainMenu / UICreateGame / UIJoinGame / UISettings
- [ ] UIInGameHUD: базовый каркас
- [ ] GameStarter + UILoadingScreen (bootstrap-сцена)
- [ ] Sandbox-редактирование не-хостом: запрос → валидация → применение

### После играбельного прототипа

- [ ] Протестировать реальный бюджет производительности ИИ на хосте (нет смысла раньше, чем
      появится что тестировать)
- [ ] Lua: бюджет исполнения (защита от `while true do end`) — требует debug-hook Lua C API;
      прерывание выполнения из хука рискованно (native longjmp через P/Invoke callback может
      уронить процесс) — отдельная, осознанная задача, не между делом. Не приоритет, пока не
      защищаемся от гриферства/недоверенных миров.

Сознательно не делаем сейчас (не открытый вопрос): мод-система шире одного мира,
дистрибуция через Steam.
